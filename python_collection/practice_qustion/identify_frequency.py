"""word = "pneumonoultramicroscopicsilicovolcanoconiosis"

set_word = set(word)

character_count = {}

for ch in set_word:

    character_count [ch] = word.count(ch)

print(character_count)
    """

array = [10,1,15,16,11,10,12,11]

array_set = set(array)

array_count = {}

for num in array_set:

    array_count [num] = array.count(num)

print(array_count)